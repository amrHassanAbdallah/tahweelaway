version: "1"
packages:
  - name: "persistence"
    path: "./"
    queries: "./queries/"
    schema: "./migration/"
    engine: "postgresql"
    emit_json_tags: true
    emit_prepared_queries: false
    emit_interface: true
    emit_exact_table_names: false